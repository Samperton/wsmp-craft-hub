# Route `/video` to the opened trailer

## Changes

**`src/routes/index.tsx`**
- Lift the trailer `Dialog`'s open state into the `Index` component (controlled). Pass `trailerOpen` + `onTrailerOpenChange` props into `Hero`, and bind them to the existing `<Dialog>`.
- Export `Index` (or a thin `IndexPage({ defaultTrailerOpen }: { defaultTrailerOpen?: boolean })` wrapper) so the `/video` route can reuse the exact same page with the dialog open on mount.
- When the dialog is closed while on `/video`, `navigate({ to: "/" })` so the URL reflects state.

**`src/routes/video.tsx`** (new)
- `createFileRoute("/video")` rendering the shared page component with `defaultTrailerOpen={true}`.
- `head()` metadata: title "What is the WSMP? — Trailer", description, og:title, og:description, og:url, and a leaf canonical link to `https://wsmp-craft-hub.lovable.app/video`.

## Notes
- `src/routeTree.gen.ts` is auto-generated by the TanStack Router Vite plugin — no manual edit needed.
- No backend or styling changes; pure routing + state-lift.
